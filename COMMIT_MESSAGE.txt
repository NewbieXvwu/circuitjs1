修复 Tauri Windows NSIS 配置错误并优化代码

主要修改：
1. 修复 tauri.conf.json 中的 NSIS 配置，移除不支持的字段
   - 移除: allowDowngrade, allowToChangeInstallationDirectory, 
     createDesktopIcon, createStartMenuShortcut, displayLanguageSelector, oneClick
   - 保留: installerIcon, installMode, languages

2. 优化 Rust 代码以消除编译警告
   - 移除未使用的 std::path::PathBuf 导入
   - 将未使用的函数参数标记为 _window

3. 验证所有配置文件语法正确

这些修复解决了 GitHub Actions 构建失败的问题。

相关错误信息：
Error `tauri.conf.json` error on `tauri > bundle > windows > nsis`: 
{...} is not valid under any of the schemas listed in the 'anyOf' keyword
